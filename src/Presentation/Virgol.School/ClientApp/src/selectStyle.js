export const styles = {
    control : (css) => ({
        ...css, 
        backgroundColor : 'transparent' ,
    }) ,

    singleValue : (css) => ({
        ...css , 
        color : 'white'
    }) ,

    Input : (css) => ({
        ...css ,
        color : 'white'
    })
}